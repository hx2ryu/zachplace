# Codex runtime notes

- Install the plugin from `marketplaces/codex/marketplace.json` or directly from the plugin directory.
- Invoke the workflow through the Codex skill picker or by mentioning `$research`.
- Use available web, browser, file, and shell capabilities; optional capabilities should degrade gracefully.
