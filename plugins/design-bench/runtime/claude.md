# Claude Code runtime notes

- Install with `claude plugin marketplace add https://github.com/hx2ryu/zachplace`.
- Install with `claude plugin install design-bench@zachplace`.
- Invoke the workflow with `/design-bench:research` or natural-language triggers.
- Use available web, browser, file, shell, and agent capabilities; optional capabilities should degrade gracefully.
